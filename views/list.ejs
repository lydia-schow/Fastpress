<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <li><a href="./create">CREATE ONE</a></li>
      <% items.forEach( item => { %>
        <li>
          <a href="./<%= item._id %>">
             <%= item.title %>
          </a>
           (<%= item._id %>)
        </li>
      <% }) %>
    </ul>
  </body>
</html>
